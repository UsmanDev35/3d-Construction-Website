import { Quote, Star } from 'lucide-react'
import Container from '../common/Container'
import SectionTitle from '../common/SectionTitle'
const quotes = [['“Working with BluePrint & Beyond completely changed how we approached development planning. Their visuals helped us avoid several expensive changes later.”', 'Michael Turner', 'Real Estate Developer'], ['“We presented the project to investors with far more confidence because they could experience the design instead of trying to imagine it.”', 'Sarah Collins', 'Architect & Design Consultant'], ['“Everyone stayed aligned throughout the project. The visualization made every detail easier to understand and sped up decisions.”', 'Daniel Brooks', 'Construction Project Manager']]
export default function Testimonials() { return <section id="testimonials" className="bg-white py-16 lg:py-24"><Container><SectionTitle eyebrow="Testimonials" title="What our clients say" /><div className="mt-10 grid gap-5 lg:grid-cols-3">{quotes.map(([quote, name, role]) => <figure key={name} className="rounded-xl border border-slate-200 p-7"><Quote className="text-brand" size={28} /><div className="mt-5 flex gap-1 text-brand">{Array.from({ length: 5 }, (_, i) => <Star key={i} size={13} fill="currentColor" />)}</div><blockquote className="mt-4 text-sm leading-7 text-slate-600">{quote}</blockquote><figcaption className="mt-6 border-t border-slate-100 pt-5 text-sm font-extrabold text-ink">{name}<span className="mt-1 block text-xs font-medium text-slate-500">{role}</span></figcaption></figure>)}</div></Container></section> }
