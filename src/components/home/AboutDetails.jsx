import { motion, useReducedMotion } from 'framer-motion'
import { ArrowRight } from 'lucide-react'
import Button from '../common/Button'
import Container from '../common/Container'
import ProjectMorphGallery from '../common/ProjectMorphGallery'

const clarityPoints = [
  ['Plan with context', 'Move beyond flat drawings with a visual environment that helps every decision-maker understand scale, material, circulation, and atmosphere.'],
  ['Align the whole team', 'Bring architects, developers, contractors, investors, and marketing teams into one shared visual conversation before decisions become costly.'],
  ['Present with confidence', 'Turn the future project into an investor-ready and buyer-ready story that is clear long before the first brick is laid.'],
]

const projectJourney = ['Brief & plans', 'Spatial model', 'Materials & lighting', 'Review & refinement', 'Presentation-ready delivery']

export default function AboutDetails() {
  const reducedMotion = useReducedMotion()
  return <section className="relative overflow-hidden bg-white py-20 lg:py-28"><div className="blueprint-grid pointer-events-none absolute inset-0 opacity-10" /><Container className="relative"><div className="grid gap-10 lg:grid-cols-[.78fr_1.22fr]"><div><p className="text-xs font-extrabold uppercase tracking-[.16em] text-brand">Why visualization changes the process</p><h2 className="mt-4 text-4xl font-extrabold leading-[1.04] tracking-[-.055em] text-ink sm:text-5xl">Better decisions start when everyone can see the same future.</h2><p className="mt-6 max-w-md text-sm leading-7 text-slate-600">A good visualization is not simply a beautiful image. It is a practical decision tool—one that helps people test an idea, understand the trade-offs, and move forward together.</p><Button to="/contact" className="group mt-8">Discuss your project <ArrowRight size={16} className="transition-transform group-hover:translate-x-1" /></Button></div><div className="grid gap-4">{clarityPoints.map(([title, copy], pointIndex) => <motion.article key={title} initial={reducedMotion ? false : { opacity: 0, x: 24 }} whileInView={reducedMotion ? {} : { opacity: 1, x: 0 }} viewport={{ once: true, amount: .2 }} transition={{ delay: pointIndex * .1, duration: .5 }} className="group relative overflow-hidden rounded-2xl border border-slate-200 bg-slate-50/70 p-6 transition duration-300 hover:-translate-y-1 hover:border-brand/40 hover:bg-white hover:shadow-xl hover:shadow-brand/10"><span className="absolute inset-y-0 left-0 w-1 bg-brand" /><span className="text-xs font-extrabold tracking-[.16em] text-brand">0{pointIndex + 1}</span><h3 className="mt-3 text-xl font-extrabold tracking-[-.03em] text-ink">{title}</h3><p className="mt-3 max-w-2xl text-sm leading-7 text-slate-600">{copy}</p></motion.article>)}</div></div><ProjectMorphGallery /><div className="mt-14 rounded-3xl bg-ink p-7 text-white shadow-2xl shadow-ink/15 sm:p-10"><div className="flex flex-col justify-between gap-6 lg:flex-row lg:items-end"><div><p className="text-xs font-extrabold uppercase tracking-[.16em] text-brand">From plan to presentation</p><h2 className="mt-3 text-3xl font-extrabold tracking-[-.045em] sm:text-4xl">A collaborative visual journey, not a one-way handoff.</h2></div><p className="max-w-sm text-sm leading-7 text-slate-300">We keep your team involved at the points where feedback has the greatest value, giving your project a clearer path from concept to launch.</p></div><ol className="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-5">{projectJourney.map((step, journeyIndex) => <motion.li key={step} initial={reducedMotion ? false : { opacity: 0, y: 16 }} whileInView={reducedMotion ? {} : { opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ delay: journeyIndex * .08 }} className="rounded-xl border border-white/10 bg-white/[.05] p-4"><span className="text-xs font-extrabold text-brand">STEP {journeyIndex + 1}</span><p className="mt-3 text-sm font-bold">{step}</p></motion.li>)}</ol></div></Container></section>
}
