import { useState } from 'react'
import { useForm } from 'react-hook-form'
import { AnimatePresence, motion, useReducedMotion } from 'framer-motion'
import { ArrowRight, Building2, CalendarDays, Clock3, LockKeyhole, Mail, MapPin, MapPinned, Phone, Send, ShieldCheck } from 'lucide-react'
import Button from '../components/common/Button'
import Container from '../components/common/Container'
import contactImage from '../assets/images/portfolio-headquarters.png'

const projectTypes = ['Residential', 'Commercial', 'Apartment', 'Hotel', 'Office Building', 'Retail', 'Industrial', 'Other']
const contactDetails = [[Building2, 'Business Name', 'BluePrint & Beyond 3D LLC'], [Mail, 'Email', 'hello@blueprintbeyond3d.com'], [Phone, 'Phone', '+1 (000) 000-0000'], [MapPin, 'Office', 'Your Business Address']]
const trustPoints = [[Clock3, 'Fast Response', 'We usually reply within 24 hours.'], [LockKeyhole, 'Project Confidentiality', 'Your architectural plans remain secure.'], [ShieldCheck, 'Professional Consultation', 'Personalized guidance before construction begins.']]

function Field({ label, name, error, register, type = 'text', required = true, options, placeholder, rows }) {
  const inputClass = `peer w-full rounded-xl border bg-white px-4 pt-6 text-sm text-ink outline-none transition placeholder:text-transparent focus:border-brand focus:ring-4 focus:ring-brand/10 ${error ? 'border-red-400' : 'border-slate-200'}`
  const registration = register(name, required ? { required: `${label} is required` } : {})
  return <div className="relative"><label htmlFor={name} className="pointer-events-none absolute left-4 top-2.5 z-10 text-[10px] font-extrabold uppercase tracking-[.12em] text-slate-500 transition peer-focus:text-brand">{label}{required && <span className="ml-0.5 text-brand">*</span>}</label>{options ? <select id={name} defaultValue="" className={`${inputClass} h-14 appearance-none`} {...registration}><option value="" disabled>Choose a project type</option>{options.map((option) => <option key={option} value={option}>{option}</option>)}</select> : rows ? <textarea id={name} rows={rows} placeholder={placeholder} className={`${inputClass} resize-y pt-7`} {...registration} /> : <input id={name} type={type} placeholder={placeholder} className={`${inputClass} h-14`} {...registration} />}{error && <p role="alert" className="mt-1.5 text-xs font-semibold text-red-600">{error.message}</p>}</div>
}

function CalendlyEmbed({ details, onBack }) {
  const params = new URLSearchParams({ hide_gdpr_survey: '1', name: details.name || '', email: details.email || '' })
  return <div><div className="flex items-start justify-between gap-5"><div><p className="text-xs font-extrabold uppercase tracking-[.16em] text-brand">Choose a time</p><h2 className="mt-2 text-3xl font-extrabold tracking-[-.045em] text-ink">Book your consultation.</h2><p className="mt-2 text-sm leading-6 text-slate-600">Select a time that works for you. You’ll receive confirmation directly from Calendly.</p></div><CalendarDays className="mt-1 shrink-0 text-brand" size={26} /></div><div className="mt-6 overflow-hidden rounded-xl border border-slate-200 bg-white"><iframe title="Schedule a consultation with BluePrint & Beyond 3D" src={`https://calendly.com/chaudharyasharabbas/30min?${params.toString()}`} className="h-[700px] w-full" loading="lazy" /></div><button type="button" onClick={onBack} className="mt-5 text-sm font-extrabold text-brand underline underline-offset-4">Back to project details</button></div>
}

export default function ContactPage() {
  const reducedMotion = useReducedMotion()
  const [stage, setStage] = useState('form')
  const [bookingDetails, setBookingDetails] = useState({})
  const { register, handleSubmit, formState: { errors, isSubmitting }, reset } = useForm({ mode: 'onBlur' })
  const words = "Let's Build Your Vision Before Construction Begins".split(' ')
  const onSubmit = async (data) => { await new Promise((resolve) => setTimeout(resolve, 550)); setBookingDetails(data); setStage('calendar'); reset() }
  return <div className="relative overflow-hidden bg-[#f8fafc]"><div className="blueprint-grid pointer-events-none absolute inset-0 opacity-20" /><section className="relative py-20 lg:py-28"><Container><div className="mx-auto max-w-5xl text-center"><motion.p initial={reducedMotion ? false : { opacity: 0, y: 12 }} animate={reducedMotion ? {} : { opacity: 1, y: 0 }} className="text-xs font-extrabold uppercase tracking-[.18em] text-brand">Let’s start the conversation</motion.p><h1 className="mt-5 text-4xl font-extrabold leading-[1.08] tracking-[-.055em] text-ink sm:text-6xl">{words.map((word, index) => <motion.span key={`${word}-${index}`} initial={reducedMotion ? false : { opacity: 0, y: 22 }} animate={reducedMotion ? {} : { opacity: 1, y: 0 }} transition={{ delay: .1 + index * .065, duration: .45 }} className="mr-[.24em] inline-block last:mr-0">{word}</motion.span>)}</h1><motion.p initial={reducedMotion ? false : { opacity: 0, y: 16 }} animate={reducedMotion ? {} : { opacity: 1, y: 0 }} transition={{ delay: .5, duration: .5 }} className="mx-auto mt-6 max-w-3xl text-base leading-7 text-slate-600">Whether you’re planning a luxury residence, commercial development, apartment complex, hotel, or architectural presentation, our visualization experts are ready to help you experience your project before the first brick is laid.</motion.p></div><div className="mt-16 grid gap-10 lg:grid-cols-[.9fr_1.1fr] lg:items-start"><motion.div initial={reducedMotion ? false : { opacity: 0, x: -26 }} whileInView={reducedMotion ? {} : { opacity: 1, x: 0 }} viewport={{ once: true, amount: .15 }} transition={{ duration: .65 }}><div className="group relative overflow-hidden rounded-2xl border border-white/70 bg-white p-2 shadow-xl shadow-ink/10"><img loading="lazy" src={contactImage} alt="Contemporary corporate architecture visualization" className="image-drift aspect-[4/3] w-full rounded-xl object-cover transition duration-700 group-hover:scale-105" /><div className="absolute inset-0 rounded-2xl bg-gradient-to-tr from-ink/35 via-transparent to-brand/10" /><div className="absolute bottom-6 left-6 rounded-xl border border-white/25 bg-ink/75 p-4 text-white backdrop-blur"><p className="text-[10px] font-extrabold uppercase tracking-[.15em] text-brand">Bring plans to life</p><p className="mt-1 text-sm font-bold">See every decision before it is built.</p></div></div><div className="mt-6 grid gap-3 sm:grid-cols-2">{contactDetails.map(([Icon, label, value], index) => <motion.div key={label} initial={reducedMotion ? false : { opacity: 0, y: 14 }} whileInView={reducedMotion ? {} : { opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ delay: index * .07 }} className="group rounded-xl border border-white/70 bg-white/75 p-4 shadow-sm backdrop-blur transition duration-300 hover:-translate-y-1 hover:border-brand/30 hover:shadow-lg hover:shadow-brand/10"><Icon size={19} className="text-brand transition-transform duration-300 group-hover:rotate-6" /><p className="mt-3 text-[10px] font-extrabold uppercase tracking-[.12em] text-slate-500">{label}</p><p className="mt-1 text-sm font-bold text-ink">{value}</p></motion.div>)}</div></motion.div><motion.div initial={reducedMotion ? false : { opacity: 0, x: 26 }} whileInView={reducedMotion ? {} : { opacity: 1, x: 0 }} viewport={{ once: true, amount: .12 }} transition={{ duration: .65 }}><div className="grid gap-3 sm:grid-cols-3">{trustPoints.map(([Icon, title, copy], index) => <motion.div key={title} initial={reducedMotion ? false : { opacity: 0, y: 12 }} whileInView={reducedMotion ? {} : { opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ delay: index * .08 }} className="rounded-xl border border-brand/10 bg-white/70 p-4 backdrop-blur"><Icon size={18} className="text-brand" /><p className="mt-3 text-xs font-extrabold text-ink">{title}</p><p className="mt-1 text-[11px] leading-5 text-slate-500">{copy}</p></motion.div>)}</div><div className="mt-5 [perspective:1400px]"><AnimatePresence mode="wait">{stage === 'calendar' ? <motion.div key="calendar" initial={reducedMotion ? false : { opacity: 0, rotateY: -180, scale: .94 }} animate={{ opacity: 1, rotateY: 0, scale: 1 }} exit={{ opacity: 0, rotateY: 180, scale: .94 }} transition={{ duration: .75, ease: [0.22, 1, 0.36, 1] }} className="rounded-2xl border border-white/80 bg-white/90 p-6 shadow-2xl shadow-ink/10 backdrop-blur sm:p-8"><CalendlyEmbed details={bookingDetails} onBack={() => setStage('form')} /></motion.div> : <motion.div key="form" initial={reducedMotion ? false : { opacity: 0, rotateY: -180, scale: .94 }} animate={{ opacity: 1, rotateY: 0, scale: 1 }} exit={{ opacity: 0, rotateY: 180, scale: .94 }} transition={{ duration: .75, ease: [0.22, 1, 0.36, 1] }} className="rounded-2xl border border-white/80 bg-white/85 p-6 shadow-2xl shadow-ink/10 backdrop-blur sm:p-8"><form onSubmit={handleSubmit(onSubmit)} noValidate><div className="flex items-start justify-between gap-6"><div><p className="text-xs font-extrabold uppercase tracking-[.16em] text-brand">Book a consultation</p><h2 className="mt-2 text-3xl font-extrabold tracking-[-.045em] text-ink">Tell us about your project.</h2></div><Send className="mt-1 shrink-0 text-brand" size={24} /></div><div className="mt-7 grid gap-5 sm:grid-cols-2"><Field label="Full Name" name="name" error={errors.name} register={register} placeholder="Your name" /><Field label="Email Address" name="email" type="email" error={errors.email} register={(name, rules) => register(name, { ...rules, pattern: { value: /^\S+@\S+\.\S+$/, message: 'Enter a valid email address' } })} placeholder="you@example.com" /><Field label="Phone Number" name="phone" type="tel" error={errors.phone} register={register} placeholder="+1 (000) 000-0000" /><Field label="Project Type" name="projectType" error={errors.projectType} register={register} options={projectTypes} /><Field label="Estimated Budget" name="budget" error={errors.budget} register={register} placeholder="Optional" required={false} /><Field label="Project Location" name="location" error={errors.location} register={register} placeholder="City, State / Country" /><div className="sm:col-span-2"><Field label="Message" name="message" error={errors.message} register={register} placeholder="Tell us a little about the project, goals, and timeline." rows={5} /></div></div><Button type="submit" disabled={isSubmitting} className="mt-7 w-full disabled:cursor-not-allowed disabled:opacity-60">{isSubmitting ? <><span className="h-4 w-4 animate-spin rounded-full border-2 border-white border-t-transparent" />Preparing your booking…</> : <>Continue to Scheduling <ArrowRight size={17} /></>}</Button><p className="mt-4 text-center text-xs leading-5 text-slate-500">Your details prefill the booking form and are used only to schedule your consultation.</p></form></motion.div>}</AnimatePresence></div></motion.div></div><motion.p initial={reducedMotion ? false : { opacity: 0, y: 14 }} whileInView={reducedMotion ? {} : { opacity: 1, y: 0 }} viewport={{ once: true }} className="mx-auto mt-16 max-w-3xl text-center font-['Playfair_Display'] text-2xl font-semibold italic leading-relaxed text-ink">“We believe every successful development begins with clear visualization. Let’s discuss your project and bring your ideas to life before construction starts.”</motion.p></Container></section><section className="relative border-t border-cyan-100 bg-white py-10"><Container><div className="grid min-h-64 place-items-center rounded-2xl border border-cyan-100 bg-[#e9fbfb] text-center"><div><span className="mx-auto grid h-14 w-14 place-items-center rounded-full bg-white text-brand shadow-lg"><MapPinned size={27} /></span><h2 className="mt-4 text-xl font-extrabold text-ink">Map coming soon</h2><p className="mt-2 text-sm text-slate-600">Your business location can be connected here when coordinates are available.</p></div></div></Container></section></div>
}
